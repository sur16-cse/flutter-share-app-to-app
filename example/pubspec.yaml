name: share_example
description: Demonstrates how to use the share plugin.

dependencies:
  flutter:
    sdk: flutter
  share:
    path: ../

flutter:
  uses-material-design: true

environment:
  sdk: ">=3.0.5 <4.0.0"
